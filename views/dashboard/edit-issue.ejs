<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Edit Issue</title>

 <style>
    body {
  margin: 0;
  font-family: "Times New Roman", Times, serif;
  min-height: 100vh;
  background:
    linear-gradient(
      rgba(0, 0, 0, 0.55),
      rgba(0, 0, 0, 0.55)
    ),
    url("/images/heroimg2.jpg");

  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

    .navbar {
      background: #360d25ef;
      padding: 15px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .navbar h2 {
      color: white;
    }

    .navbar ul {
      list-style: none;
      display: flex;
      gap: 25px;
    }

    .navbar a {
      text-decoration: none;
      color: white;
      font-weight: bold;
      cursor: pointer;
    }

    /* PAGE */
    .container {
      padding: 40px;
    }

    .section {
      display: none;
      background:  rgba(255, 255, 255, 0.877);
      padding: 30px;
      border-radius: 15px;
      margin-bottom: 20px;
    }

    .section.active {
      display: block;
    }

    h3 {
      color:  #360d25ef;
    }

    input, textarea, select {
      width: 100%;
      padding: 12px;
      margin-bottom: 15px;
      font-size: 15px;
    }

    button {
      background: #360d25ef;
      color:  white;
      border: none;
      padding: 12px 18px;
      cursor: pointer;
      border-radius: 6px;
    }

    .issue {
      border-bottom: 1px solid #ccc;
      margin-bottom: 15px;
      padding-bottom: 10px;
    }

    .section p {
  font-size: 17px;
  line-height: 1.6;
}

.issue p {
  font-size: 16.5px;
}

h3 {
  font-size: 24px;
}

  </style>
</head>

<div class="navbar">
  <h2>SafeVoice</h2>
  <ul>
    <li><a onclick="showSection('home')">Home</a></li>
    <li><a onclick="showSection('report')">Report Issue</a></li>
    <li><a onclick="showSection('status')">My Issues</a></li>
    <li><a href="/logout">Logout</a></li>
  </ul>
</div>

<div class="container">
<div class="section">
<h3>Edit Issue</h3>

<form method="POST" action="/user/issue/update">
  <input type="hidden" name="issue_id" value="<%= issue.issue_id %>">

  <input name="title" value="<%= issue.title %>" required>
  <textarea name="description" required><%= issue.description %></textarea>

  <select name="category" required>
    <option <%= issue.category==="Harassment"?"selected":"" %>>Harassment</option>
    <option <%= issue.category==="Domestic Violence"?"selected":"" %>>Domestic Violence</option>
    <option <%= issue.category==="Cyber Crime"?"selected":"" %>>Cyber Crime</option>
  </select>

  <button type="submit">Update Issue</button>
</form>
</div>
</div>
</body>
</html>
